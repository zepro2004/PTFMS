# Example database configuration (copy to database.properties and adjust)
# For Docker usage, these will typically be overridden by environment variables.
# jdbc.url is constructed from DB_HOST and DB_NAME if you externalize it in code.

jdbc.url=jdbc:mysql://localhost:3306/ptfms
jdbc.username=ptfms_user
jdbc.password=changeMeStrongPassword
